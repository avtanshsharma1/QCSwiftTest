// BadFormatting.swift – intentionally poorly formatted to trigger SwiftFormat

import Foundation

 struct  BadFormatter  {
 let  value:Int
 func  show( ){
   if(value>0){print("Positive")}
   else{print ( "Non-positive" )}
 }
 }
 
// Trailing spaces and mixed indentation below intentionally…………………… 